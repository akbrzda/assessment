/**
 * Логирование временно отключено.
 * Сервис оставлен как единая точка входа для модулей и legacy-кода.
 */
async function createLog() {
  return Promise.resolve();
}

module.exports = {
  createLog,
};
