# Фаза 4 — Отчет EXPLAIN (реальная БД)

Дата запуска: 2026-04-22  
Команда: `cd backend && npm run phase4:explain`

## Сводка

- Проверено 5 критичных запросов.
- Все запросы выполнились успешно.
- На запросах по назначениям используется индекс `uniq_assessment_user`.
- Для двух запросов получен результат `no matching row in const table` (на текущих данных строка не найдена, но план подтверждает константный доступ по ключу).

## Ключевые наблюдения

1. Список аттестаций пользователя
- План: `JOIN` c `assessment_user_assignments`.
- Использование: `eq_ref` + индекс `uniq_assessment_user`.
- Риск: сортировка через `using_filesort` из-за `ORDER BY a.open_at DESC`.

2. Детали аттестации для менеджера
- План: `const table` (по `PRIMARY`).
- На тестовых параметрах запись не найдена.

3. Незавершенные попытки пользователя
- План: доступ `ref` по индексу `fk_assessment_attempt_user`.
- Риск: сортировка `using_filesort` по `started_at`.

4. Назначения пользователя по фильтру аттестации
- План: `const table` с проверкой пары `(assessment_id, user_id)`.
- На тестовых параметрах запись не найдена.

5. Просроченные попытки для автозавершения
- План: скан `assessment_attempts` по условию `status = 'in_progress'`, затем `eq_ref` к `assessments`.
- Рекомендация: при росте данных рассмотреть отдельный составной индекс на попытках для ускорения фоновой выборки.

## Вывод

- Базовая индексация для критичных сценариев рабочая.
- На текущем объеме данных проблем с планами не выявлено.
- Следующий шаг при росте нагрузки: добавить профильный индекс под фоновый запрос автозавершения.
